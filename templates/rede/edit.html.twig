{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block body %}

    <h1 class="page-header"> Editar rede {{ rede.nome }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary"> Editar </button>
    {{ form_end(form) }}

{% endblock %}
